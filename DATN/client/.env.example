# Chỉ cần khi build production (deploy).
# Dev để trống: Vite proxy '/api' sang http://localhost:5000 (xem vite.config.ts).
#
# Đặt origin của backend, KHÔNG kèm '/api' — axios tự thêm.
# Ví dụ: VITE_API_URL=https://noithat-api.onrender.com
VITE_API_URL=
